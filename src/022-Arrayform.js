const arr = {id:'007',name:'zhangsan',age:'17'}

const result = Array.from(Object.values(arr))

console.log(result)


let ky = {if:'',name:'',gender:''}

let {id,name,age,gender} = arr

console.log(age)
